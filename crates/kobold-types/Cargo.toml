[package]
name = "kobold-types"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0"
description = "Pure data models, interfaces, and trait contracts for Kobold thin harness"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
futures-core = "0.3"
async-trait = "0.1"
thiserror = "2"
